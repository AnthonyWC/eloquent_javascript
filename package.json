{
  "name": "chap2",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "lint": "DIR=${DIR:-chap2}; eslint $DIR/src/*.js; file $DIR/babel | grep -q 'cannot open' && echo 'no babel directory' || eslint $DIR/babel/*.js",
    "test": "DIR=${DIR:-chap2}; ava $DIR/test/test*.js --verbose",
    "build": "DIR=${DIR:-chap2}; file $DIR/babel | grep -q 'cannot open' && echo 'no babel directory' || babel $DIR/babel/*.js -d $DIR/babel/src"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "@babel/register": "^7.0.0",
    "ava": "^0.25.0",
    "eslint": "^5.7.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "npm-run-all": "^4.1.3"
  },
  "ava": {
    "require": [
      "@babel/register"
    ],
    "author": "",
    "license": "ISC"
  }
}
